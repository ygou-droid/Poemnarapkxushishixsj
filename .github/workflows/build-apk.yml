```yaml
name名称: Build APK

on:触发:
  push:推送:
    branches: [ main ]分支: [main]分支: [main]主]分支: [main]主]主]
  workflow_dispatch:工作流调度:

jobs:作业:
  build:构建:
    runs-on: ubuntu-latest运行环境: ubuntu-latest
    steps:步骤步骤:步骤:
      - name: Checkout      - 名称: 检出
        uses: actions/checkout@v4用途: actions/checkout@v4

      - name: Set up JDK 17      - 名称: 设置 JDK 17
        uses: actions/setup-java@v4用途: actions/setup-java@v4
        with:参数:
          java-version: '17'Java 版本: '17'
          distribution: 'temurin'发行版: 'temurin'

      - name: Setup Gradle      - 名称: 配置 Gradle
        uses: gradle/actions/setup-gradle@v4用途: gradle/actions/setup-gradle@v4
        with:参数:
          gradle-version: 8.5

      - name: Build Debug APK      - 名称: 构建调试 APK
        run: gradle assembleDebug运行: gradle assembleDebug

      - name: Upload APK      - name: 上传APK - 名称: 上传APK - 名称: 上传APK
        uses: actions/upload-artifact@v4用途: aactions/upload-artifact@v4用途: actions/upload-artifact@v4用途: actions/upload-artifact@v4
        with:参数:
          name: StoryPhone-APK名称: S故事电话-APK名称: StoryPhone-APK
          path路径: app/build/outputs/apk/debug/app-debug.apk路径: app/build/outputs/apk/debug/app-debug.apk
```
